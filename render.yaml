services:
  - type: web
    name: descargador-universal-pro
    runtime: docker
    plan: free
    autoDeploy: true
    branch: main
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: TMP_DIR
        value: /tmp/ytdl
